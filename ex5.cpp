#include <iostream>
#include <cstdint> 


int main()
{
    double sum{};
    int32_t count{};
    if (static_cast<double>(sum) / count != 0.0)
    {
        std::cout << "Среднее арифметическое элементов между максимальным и минимальным: " << CalculateAverage( arr[],  size, sum,  count) << std::endl;
    }

    return 0;
}

